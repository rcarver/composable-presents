import XCTest
@testable import composable_optionality

final class composable_optionalityTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(composable_optionality().text, "Hello, World!")
    }
}
